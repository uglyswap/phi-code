{
	"name": "@phi-code-admin/pods",
	"version": "0.57.0",
	"description": "CLI tool for managing vLLM deployments on GPU pods",
	"type": "module",
	"bin": {
		"phi-pods": "dist/cli.js"
	},
	"scripts": {
		"clean": "shx rm -rf dist",
		"build": "tsgo -p tsconfig.build.json && shx chmod +x dist/cli.js && shx cp src/models.json dist/ && shx cp -r scripts dist/",
		"prepublishOnly": "npm run clean && npm run build"
	},
	"files": [
		"dist",
		"scripts",
		"CHANGELOG.md"
	],
	"keywords": [
		"llm",
		"vllm",
		"gpu",
		"ai",
		"cli"
	],
	"author": "Mario Zechner",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/uglyswap/phi-code.git",
		"directory": "packages/pods"
	},
	"engines": {
		"node": ">=20.0.0"
	},
	"dependencies": {
		"chalk": "^5.5.0"
	},
	"devDependencies": {}
}
